<%#
# Key: subagents/tools/send/inputs/send_mode/description
# Purpose: Subagent follow-up mode input description.
# Used: Included in a Tool or Tool-input specification shown to the model as the subagent follow-up mode input description.
# Locals:
# - None.
# Constraints:
# - Must render non-blank.
-%>
sync waits for this turn; async enqueues it and returns immediately.
