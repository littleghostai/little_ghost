<%#
# Key: subagents/feedback/unknown_id
# Purpose: Unknown subagent identity feedback.
# Used: Returned to the model when LittleGhost produces unknown subagent identity feedback.
# Locals:
# - id: Program or subagent identifier referenced by the message.
# Constraints:
# - Must render non-blank.
-%>
Unknown subagent id: <%= id %>
