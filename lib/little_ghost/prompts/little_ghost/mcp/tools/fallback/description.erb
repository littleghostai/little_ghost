<%#
# Key: mcp/tools/fallback/description
# Purpose: MCP Tool fallback description.
# Used: Supplies a Tool description when an MCP server does not provide one.
# Locals:
# - server_name: Display name of the MCP server.
# Constraints:
# - Must render non-blank.
-%>
MCP Tool from <%= server_name %>
