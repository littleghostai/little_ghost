<%#
# Key: sandbox/filesystem/feedback/text_multiple
# Purpose: Ambiguous sandbox replacement text feedback.
# Used: Returned to the model when LittleGhost produces ambiguous sandbox replacement text feedback.
# Locals:
# - path: Path or validation location referenced by the prompt.
# Constraints:
# - Must render non-blank.
-%>
Text occurs more than once in <%= path %>
