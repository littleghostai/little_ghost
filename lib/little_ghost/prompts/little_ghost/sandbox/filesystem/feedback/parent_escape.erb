<%#
# Key: sandbox/filesystem/feedback/parent_escape
# Purpose: Sandbox parent-path escape feedback.
# Used: Returned to the model when LittleGhost produces sandbox parent-path escape feedback.
# Locals:
# - None.
# Constraints:
# - Must render non-blank.
-%>
Path escapes the sandbox scope
