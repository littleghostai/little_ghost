<%#
# Key: skills/resources/notices/truncated
# Purpose: Truncated skill-resource listing marker.
# Used: Inserted into larger model-visible content as the truncated skill-resource listing marker.
# Locals:
# - max_files: Maximum number of resource paths included.
# Constraints:
# - Must render non-blank.
-%>
... (truncated at <%= max_files %> files)
