<%#
# Key: code_mode/errors/cleanup_failed
# Purpose: Code-mode cleanup failure feedback.
# Used: Returned to the model when LittleGhost produces code-mode cleanup failure feedback.
# Locals:
# - None.
# Constraints:
# - Must render non-blank.
-%>
The code-mode session cannot be reused after cleanup failed.
