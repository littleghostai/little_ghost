<%#
# Key: code_mode/tools/wait/description
# Purpose: Code-mode wait Tool description.
# Used: Included in a Tool or Tool-input specification shown to the model as the code-mode wait Tool description.
# Locals:
# - None.
# Constraints:
# - Must render non-blank.
-%>
Observe the active code-mode program for up to one minute. The program keeps running continuously; wait does not resume or restart it. Call again only after exec or wait returned still_working.
