<%#
# Key: code_mode/feedback/limit_exceeded
# Purpose: Code-mode limit feedback.
# Used: Returned to the model when LittleGhost produces code-mode limit feedback.
# Locals:
# - limit: Name or value of the exceeded limit.
# Constraints:
# - Must render non-blank.
-%>
The code-mode <%= limit %> limit was exceeded.
