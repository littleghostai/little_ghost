<%#
# Key: code_mode/feedback/program_active
# Purpose: Existing active code-mode program feedback.
# Used: Returned to the model when LittleGhost produces existing active code-mode program feedback.
# Locals:
# - None.
# Constraints:
# - Must render non-blank.
-%>
Wait for or stop the active code-mode program before starting another.
