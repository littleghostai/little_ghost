<%#
# Key: code_mode/feedback/closed
# Purpose: Closed code-mode resource feedback.
# Used: Returned to the model when LittleGhost produces closed code-mode resource feedback.
# Locals:
# - resource: Code-mode resource that is no longer available.
# Constraints:
# - Must render non-blank.
-%>
The code-mode <%= resource %> is closed.
