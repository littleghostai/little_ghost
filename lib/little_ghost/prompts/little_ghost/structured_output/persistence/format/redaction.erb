<%#
# Key: structured_output/persistence/format/redaction
# Purpose: Persisted structured-result placeholder.
# Used: Inserted into larger model-visible content as the persisted structured-result placeholder.
# Locals:
# - schema_name: Name of the required structured-result schema or Tool.
# Constraints:
# - Must render non-blank.
# - This output is embedded inside a larger model-visible message.
# - Keep the wording consistent with LittleGhost's enforced structured-result protocol.
-%>
[Structured result <%= schema_name %> redacted]
