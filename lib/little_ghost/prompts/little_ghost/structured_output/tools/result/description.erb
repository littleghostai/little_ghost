<%#
# Key: structured_output/tools/result/description
# Purpose: Terminal structured-result Tool description.
# Used: Included in a Tool or Tool-input specification shown to the model as the terminal structured-result Tool description.
# Locals:
# - None.
# Constraints:
# - Must render non-blank.
# - Keep the wording consistent with LittleGhost's enforced structured-result protocol.
-%>
Submit the final structured result. Call this tool exactly once and only as the final action.
