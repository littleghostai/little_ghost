<%#
# Key: structured_output/validation/feedback/invalid_json
# Purpose: Invalid structured-result JSON feedback.
# Used: Returned to the model when LittleGhost produces invalid structured-result JSON feedback.
# Locals:
# - None.
# Constraints:
# - Must render non-blank.
# - Keep the wording consistent with LittleGhost's enforced structured-result protocol.
-%>
The structured result is not valid JSON.
