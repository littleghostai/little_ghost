<%#
# Key: structured_output/validation/feedback/too_large
# Purpose: Oversized structured-result feedback.
# Used: Returned to the model when LittleGhost produces oversized structured-result feedback.
# Locals:
# - None.
# Constraints:
# - Must render non-blank.
# - Keep the wording consistent with LittleGhost's enforced structured-result protocol.
-%>
The structured result exceeds the maximum serialized size.
