<%#
# Key: structured_output/validation/feedback/tool_not_exclusive
# Purpose: Non-exclusive structured-result Tool validation feedback.
# Used: Returned to the model when LittleGhost produces non-exclusive structured-result Tool validation feedback.
# Locals:
# - None.
# Constraints:
# - Must render non-blank.
# - Keep the wording consistent with LittleGhost's enforced structured-result protocol.
-%>
The structured result Tool must be the only Tool call in its response.
