<%#
# Key: tools/validation/schema/enum
# Purpose: Schema enum validation feedback.
# Used: Returned to the model when LittleGhost produces schema enum validation feedback.
# Locals:
# - path: Path or validation location referenced by the prompt.
# - values: Allowed JSON Schema enum values.
# Constraints:
# - Must render non-blank.
-%>
<%= path %> must be one of <%= values.map(&:inspect).join(", ") %>
