<%#
# Key: tools/validation/schema/additional_property
# Purpose: Schema additional-property feedback.
# Used: Returned to the model when LittleGhost produces schema additional-property feedback.
# Locals:
# - path: Path or validation location referenced by the prompt.
# Constraints:
# - Must render non-blank.
-%>
<%= path %> is not allowed
