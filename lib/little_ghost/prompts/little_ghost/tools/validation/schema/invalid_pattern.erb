<%#
# Key: tools/validation/schema/invalid_pattern
# Purpose: Schema pattern-definition feedback.
# Used: Returned to the model when LittleGhost produces schema pattern-definition feedback.
# Locals:
# - path: Path or validation location referenced by the prompt.
# Constraints:
# - Must render non-blank.
-%>
<%= path %> has an invalid schema pattern
