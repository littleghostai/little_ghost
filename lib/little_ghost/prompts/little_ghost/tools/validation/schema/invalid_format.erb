<%#
# Key: tools/validation/schema/invalid_format
# Purpose: Schema string-format feedback.
# Used: Returned to the model when LittleGhost produces schema string-format feedback.
# Locals:
# - path: Path or validation location referenced by the prompt.
# Constraints:
# - Must render non-blank.
-%>
<%= path %> has an invalid format
