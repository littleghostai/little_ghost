<%#
# Key: tools/validation/schema/max_length
# Purpose: Schema string maximum-length feedback.
# Used: Returned to the model when LittleGhost produces schema string maximum-length feedback.
# Locals:
# - path: Path or validation location referenced by the prompt.
# - maximum: Configured maximum allowed by the relevant limit.
# Constraints:
# - Must render non-blank.
-%>
<%= path %> must have at most <%= maximum %> characters
