<%#
# Key: tools/validation/schema/max_items
# Purpose: Schema array maximum-size feedback.
# Used: Returned to the model when LittleGhost produces schema array maximum-size feedback.
# Locals:
# - path: Path or validation location referenced by the prompt.
# - maximum: Configured maximum allowed by the relevant limit.
# Constraints:
# - Must render non-blank.
-%>
<%= path %> must contain at most <%= maximum %> items
