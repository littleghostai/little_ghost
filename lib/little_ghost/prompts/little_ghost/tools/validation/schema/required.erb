<%#
# Key: tools/validation/schema/required
# Purpose: Schema required-property feedback.
# Used: Returned to the model when LittleGhost produces schema required-property feedback.
# Locals:
# - path: Path or validation location referenced by the prompt.
# Constraints:
# - Must render non-blank.
-%>
<%= path %> is required
