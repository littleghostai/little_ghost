<%#
# Key: workspace/feedback/path_changed
# Purpose: Changed workspace-path feedback.
# Used: Returned to the model when LittleGhost produces changed workspace-path feedback.
# Locals:
# - name: Name of the Tool, skill, path, or resource referenced by the prompt.
# Constraints:
# - Must render non-blank.
-%>
workspace path changed after opening: <%= name %>
