<%#
# Key: artifacts/batch/feedback/batch_too_many
# Purpose: Artifact batch-count limit feedback.
# Used: Returned to the model when LittleGhost produces artifact batch-count limit feedback.
# Locals:
# - maximum: Configured maximum allowed by the relevant limit.
# Constraints:
# - Must render non-blank.
-%>
Artifact batch exceeds the <%= maximum %>-item limit
