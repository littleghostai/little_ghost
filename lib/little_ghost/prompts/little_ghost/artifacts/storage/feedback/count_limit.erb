<%#
# Key: artifacts/storage/feedback/count_limit
# Purpose: Stored artifact count-limit feedback.
# Used: Returned to the model when LittleGhost produces stored artifact count-limit feedback.
# Locals:
# - maximum: Configured maximum allowed by the relevant limit.
# Constraints:
# - Must render non-blank.
-%>
Artifact count exceeds the <%= maximum %>-item limit
