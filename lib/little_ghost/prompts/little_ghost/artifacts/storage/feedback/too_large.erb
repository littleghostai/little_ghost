<%#
# Key: artifacts/storage/feedback/too_large
# Purpose: Stored artifact size-limit feedback.
# Used: Returned to the model when LittleGhost produces stored artifact size-limit feedback.
# Locals:
# - maximum: Configured maximum allowed by the relevant limit.
# Constraints:
# - Must render non-blank.
-%>
Artifact exceeds the <%= maximum %>-byte limit
