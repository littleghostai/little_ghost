<%#
# Key: artifacts/presentation/format/workspace_references
# Purpose: Workspace artifact reference appendix.
# Used: Inserted into larger model-visible content as the workspace artifact reference appendix.
# Locals:
# - references: Collection of model-visible artifact references.
# Constraints:
# - Must render non-blank.
# - This output is embedded inside a larger model-visible message.
-%>
Workspace artifacts:
<%= references.map { |reference| "- #{reference}" }.join("\n") %>
